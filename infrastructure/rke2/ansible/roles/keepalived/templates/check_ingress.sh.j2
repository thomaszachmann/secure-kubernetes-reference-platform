#!/bin/bash
# Health check script for Ingress Controller
# Checks if ingress controller is listening on port 443 or 80
# Exit code 0 = healthy, non-zero = unhealthy

# Check if any process is listening on port 443 or 80
ss -tlnp | grep -E ':(443|80)\s' > /dev/null 2>&1
exit $?
